---
// import { Markdoc, readDir } from "@/mdoc-plugin";
// import { CONSTANTS } from "@/utilities/env";
// import type { RenderableTreeNodes } from "@markdoc/markdoc";

// export async function getStaticPaths() {
// 	const allPosts = await readDir(CONSTANTS.content);
// 	const posts = allPosts.filter(({ meta }) => meta.draft !== true);

// 	return posts.map((post, i) => ({
// 		params: { post: post.slug },
// 		props: {
// 			post: post.tree,
// 			frontmatter: post.meta,
// 			pagination: {
// 				previous: i !== 0 ? posts.at(i - 1) : undefined,
// 				next: posts.at(i + 1),
// 			},
// 		},
// 	}));
// }

// type ReadReturnType = Awaited<ReturnType<typeof readDir>>[number];

// export interface Props {
// 	post: RenderableTreeNodes;
// 	frontmatter: Pick<ReadReturnType, "meta">;
// 	pagination: {
// 		previous: ReadReturnType | undefined;
// 		next: ReadReturnType | undefined;
// 	};
// }

// const { post } = Astro.props;
---

<!-- <Markdoc content={post} /> -->
